---
title: Browse Components
description: Browse the library of customizable, framework-friendly web components included in Cornerstone.
# One flat grid with no headings, so there is nothing to outline — and the reclaimed column is what
# brings the cards to upstream's 322px. See the rule in docs.css keyed on `html:not([data-has-toc])`.
tableOfContents: false
# No sidebar: the grid is the components navigation, and a tree listing all 70 beside a grid
# listing all 70 says everything twice. Upstream's browse page has none either. `splash` is
# Starlight's own switch — see the rule keyed on `html:not([data-has-sidebar])` in docs.css.
template: splash
---
