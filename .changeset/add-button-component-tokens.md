---
"@cruglobal/cornerstone-design-system": minor
---

Add `_cmp/button` component tokens (158 leaves across primary, secondary, information, success, warning, danger, disabled variants in filled/outlined/ghost styles, plus space tokens). Pulled via the new diff-detecting `/pull-tokens` flow as a single targeted extraction — first end-to-end validation that the pipeline localizes a component-level edit to one file.
