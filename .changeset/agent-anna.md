---
"@cruglobal/cornerstone-design-system": minor
---

Add Anna, the docs-and-stories persona, at `plugins/cornerstone-dev/agents/anna.md` — one of the five agents from the persona architecture spec (#66, model floor per #74).

Anna owns documentation and Storybook end to end. Per #66 that includes all four Storybook tickets the #60 map originally called out of scope: she **is** the story generator (UIUX-89 — agentic, writes the story directly when invoked, with no separate scaffold CLI); she authors and maintains UIUX-90's coverage check and UIUX-92's style tests as **deterministic scripts** that run as plain Node with a non-zero exit and no model in the loop, which preserves UIUX-90's own rationale for living in code; she performs UIUX-91's backfill sweep herself once components exist; and she owns visual regression as a distinct concern from UIUX-92, since a pixel diff and a token assertion fail for different reasons. Joseph never writes a `.stories.js` file (#66 struck Storybook wiring from his bootstrap), and Anna's own Storybook bootstrap is human-sign-off gated but not sequenced behind the other personas existing.

No `model:` key — #74 sets her floor to inherit, and omitting the key is how that's expressed. No skill pointers: #73 supersedes #62 and leaves Joseph, Sarah, Esther and Anna with none of their own.

Written against the repo rather than the spec's summary of it, so the file carries mechanism a reader can act on: `CHANGELOG.md` is generated by `changeset version` under `changesets/action` and formatted by `@changesets/changelog-github`, so her lever on a release note is the changeset prose and hand-written PR links would double up; the `changeset` CI job checks coverage rather than substance, which is why "no empty changesets" is persona policy instead of a CI rule; `npm run lint` is `eslint build.mjs scripts/` — an explicit path list, not a repo-wide glob — so her check scripts belong in `scripts/` or they go silently unlinted; CI has no test job and the repo has no test runner at all, so Vitest is the first one rather than a choice against Jest; and `.gitignore` already ignores `storybook-static/`, which makes committing visual-regression baselines outside it a correctness requirement, since a missing baseline makes a snapshot check pass forever.

Her "state why a dependency exists" principle (X needs Y because Z) is applied in the file, not just stated: the Vite ≥ 5 → `@storybook/web-components-vite` → Vitest chain each carry their reason, and rather than naming a snapshot of current README drift, which any sibling PR can fix out from under her, she is pointed at the plugin manifests as the source of truth to reconcile prose against — the check that keeps working after today's drift is gone.
