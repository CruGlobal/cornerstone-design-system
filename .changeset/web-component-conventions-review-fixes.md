---
"@cruglobal/cornerstone-design-system": patch
---

Address review feedback on the web component API conventions doc: fix the icon font name (Material Symbols Sharp, not Outlined) and clarify that `variant`/`appearance` are two of several planned per-component attribute axes (`size`/`state` still to come), not the complete set.
